{% extends '::base.html.twig' %}

{% block body %}
    <article>
        <header>
            <h1>Article: {{ entity.title }}</h1>
            <aside>
                <ul>
                    <li>Date created: <time>{{ entity.dateCreated|date('Y-m-d H:i:s') }}</time></li>
                    <li>Date updated: <time>{{ entity.dateUpdated|date('Y-m-d H:i:s') }}</time></li>
                    <li>Date deleted: <time>{{ entity.dateDeleted|date('Y-m-d H:i:s') }}</time></li>
                </ul>
            </aside>
        </header>
        <p>{{ entity.content }}</p>

        <ul class="record_actions">
            <li>
                <a href="{{ path('article') }}">
                    Back to the list
                </a>
            </li>
            <li>
                <a href="{{ path('article_edit', { 'slug': entity.slug }) }}">
                    Edit
                </a>
            </li>
            <li>
                <form action="{{ path('article_delete', { 'slug': entity.slug }) }}" method="post">
                    {{ form_widget(delete_form) }}
                    <button type="submit">Delete</button>
                </form>
            </li>
        </ul>
    </article>
{% endblock %}
